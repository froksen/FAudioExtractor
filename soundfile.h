#ifndef SOUNDFILE_H
#define SOUNDFILE_H

#include <QObject>

class SoundFile : public QObject
{
    Q_OBJECT
public:
    explicit SoundFile(QObject *parent = 0);
    
signals:
    
public slots:
    
};

#endif // SOUNDFILE_H
